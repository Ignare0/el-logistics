{
  "name": "el-logistics-root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm run build --filter apps/server",
    "test": "pnpm run test --filter apps/server"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
